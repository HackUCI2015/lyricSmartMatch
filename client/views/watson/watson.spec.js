'use strict';

describe('Controller: WatsonCtrl', function () {

  beforeEach(module('hackuci2015'));

  var WatsonCtrl;

  beforeEach(inject(function ($controller) {
    WatsonCtrl = $controller('WatsonCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
